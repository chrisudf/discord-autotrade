[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    integration: 需要真实外部服务(OpenD/Discord/Telegram)的交互测试,默认不跑
addopts = -m "not integration"
filterwarnings =
    ignore::DeprecationWarning
